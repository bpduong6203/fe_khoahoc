import React from "react";
import ResetPassword from "@/layouts/settings/reset-password";
import SettingsLayout from "@/layouts/settings/layout";

export default function PassSettings() {
  return (
    <SettingsLayout>
      <ResetPassword />
    </SettingsLayout>
  );
}